--- tests/FieldDataTest.php
+++ tests/FieldDataTest.php
@@ -239,4 +239,18 @@
         $this->assertSame('Link 2', trim($items[1]->text));
         $this->assertSame('<a href="href2.com">Link 2</a>', trim($items[1]->html));
     }
+
+    public function test_get_iterator_returns_items()
+    {
+        $field = new FieldData(
+            name: 'test',
+            markdown: '',
+            html: '',
+            text: '',
+            type: 'list',
+            data: [['text' => 'Item 1']]
+        );
+
+        $this->assertSame($field->items(), $field->getIterator());
+    }
 }
